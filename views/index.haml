%section{:id => "main"}
  .container
    .content
      .row
        .span12
          = render(:haml, :'_all')
      .row
        .span6
          = render(:haml, :'_summary')
        .span3
          = render(:haml, :'_summary15')
        .span3
          = render(:haml, :'_summary30')
