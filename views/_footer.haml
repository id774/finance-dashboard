%section#footer
  .container
    %footer.footer
      %hr
      %p{:class=>"pull-right"}
        = render(:haml, :'_links_r')
      %p
        = render(:haml, :'_links')
        = render(:haml, :'_links_c')
