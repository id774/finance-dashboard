%section{:id => "main"}
- if @data.length > 0
  .container
    .content
      .row
        .span9
          = render(:haml, :'_chart')
          = render(:haml, :'_ti')

        .span3
          = render(:haml, :'_series')

= render(:haml, :'_footer')
