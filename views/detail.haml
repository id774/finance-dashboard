%section{:id => "main"}
- if @data.length > 0
  .container
    .content
      .row
        .span8
          = render(:haml, :'_chart')

        .span4
          = render(:haml, :'_series')

  .container
    .content
      .row
        .span12
          = render(:haml, :'_ti')

= render(:haml, :'_footer')
